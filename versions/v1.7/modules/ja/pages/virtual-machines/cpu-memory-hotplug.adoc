= CPUおよびメモリのホットプラグ
:revdate: 2025-09-16
:page-revdate: {revdate}

{harvester-product-name}は仮想マシンのためにCPUおよびメモリのホットプラグをサポートしています。この機能により、再起動を必要とせずに、稼働中の仮想マシンのCPUおよびメモリリソースを増やすことができます。

[NOTE]
====
ARM64アーキテクチャはメモリのホットプラグのみをサポートしています。CPUホットプラグ操作には、仮想マシンを再起動する必要があります。
====

== 仮想マシン作成時にCPUおよびメモリのホットプラグを有効にする

. *仮想マシン:作成*画面で、割り当てるCPUコア数とメモリを指定します。
+
image::vm/enable-cpu-and-memory-hotplug.png[CPUおよびメモリのホットプラグを有効にする]

. *CPUおよびメモリのホットプラグを有効にする*を選択します。
+
{harvester-product-name}は*最大CPU*および*最大メモリ*フィールドを`max-hotplug-ratio`設定に基づいて自動的に入力します。これらの値は、仮想マシンが実行中に利用可能な最大のCPUおよびメモリリソースの量を表します。必要に応じて、これらの値を変更することができます。

. 仮想マシン作成の残りの手順を完了します。

== CPUおよびメモリのホットプラグアクションをトリガーする

. 仮想マシンが作成された後、*Edit CPU and Memory*ボタンを使用して、CPUおよびメモリリソースを動的に追加できます。
+
image::vm/edit-cpu-and-memory.png[CPUおよびメモリリソースの増加]

. 仮想マシンを構成されたCPUおよびメモリ量を持つノードに移行するために、*適用*をクリックしてください。

[NOTE]
====
仮想マシンはxref:virtual-machines/live-migration.adoc#_live_migratable_virtual_machines[ライブマイグレーション可能]でなければなりません。
====

== `max-hotplug-ratio`設定

xref:installation-setup/config/settings.adoc#_max_hotplug_ratio[`max-hotplug-ratio`]は、稼働中の仮想マシンに利用可能なデフォルトの最大CPUおよびメモリリソースの量を決定するグローバル設定です。この比率は、仮想マシンを作成した際に割り当てたCPUおよびメモリリソースの量に掛けられます。

[sidebar]
実行時の最大利用可能リソース = （仮想マシン作成時に割り当てたリソース）×（`max-hotplug-ratio`の値）

`1`から`20`までの値を指定できます。デフォルト値は、`4`です。

例:

`max-hotplug-ratio`は`4`に設定されています。

|===
| リソースの作成 | 割り当てられた量 | 最大利用可能量

| CPUコア
| `1`
| `4`

| メモリ（Gi）
| `2`
| `8`
|===

image::vm/edit-max-hotplug-ratio.png[max-hotplug-ratio設定]